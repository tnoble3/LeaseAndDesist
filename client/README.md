# Client (Vite + React)

Quick frontend for LeaseAndDesist.

How to run:

1. cd client
2. npm install
3. npm run dev

The dev server proxies `/api` to `http://localhost:5000` (set in `vite.config.js`).

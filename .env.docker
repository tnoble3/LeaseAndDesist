# Docker-friendly environment variables for docker-compose
# Mongo uses the service name 'mongo' on the compose network
MONGO_URI=mongodb://mongo:27017/leaseanddesist
# Provide a JWT secret for signing tokens
JWT_SECRET=replace_this_with_a_secure_random_value
# Backend port (optional)
PORT=5000
